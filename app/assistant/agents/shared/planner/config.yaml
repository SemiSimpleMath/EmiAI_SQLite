name: shared::planner
class_name: Planner
color: "green"
llm_params:
  llm_provider: "openai"
  engine: "gpt-5"
  temperature: 1
  api_key: "dummy_api_key"

allowed_tools: all
except_tools: [emi_team]
allowed_nodes: [shared::writer, shared::mathematician, flow_exit_node]

user_context_items:
  - date_time
  - task
  - information
  - recent_history
  - checklist
  - action_count


rag_fields:
  task: ["chat"]
  information: ["chat"]

system_context_items:
  - tool_descriptions
  - allowed_nodes
  - task




