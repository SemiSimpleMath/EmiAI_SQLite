**User Query:** {{ query }}

**JSON Finder Results:**
Found Locations: {{ found_locations }}
Suggested Insert Path: {{ suggested_insert_path }}

**Current JSON:**
```json
{{ json_content }}
```

Decide what edits to make. Return a list of operations (delete/update/insert) or reject if no change needed.


