name: playwright::watchdog
class_name: PlaywrightWatchdog
color: "yellow"
llm_params:
  llm_provider: "openai"
  engine: "gpt-4o-mini"
  temperature: 0

allowed_tools: []
except_tools: []

system_context_items: []

user_context_items:
  - date_time
  - recent_history
  # Watchdog runs after tool execution; focus on recent_history only.

